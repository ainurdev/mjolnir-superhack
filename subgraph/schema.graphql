type Stream @entity(immutable: true) {
  id: String!
  isPrivate: Boolean!
  cid: String!
}

type Station @entity(immutable: true) {
  id: String!
  owner: String!
  cid: String!
  monthlyFee: BigInt!
  stream: Stream
}

type NFTSubscription @entity(immutable: true) {
  id: String!
  station: Station!
  owner: String!
}
